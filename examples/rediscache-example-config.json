/*
{
  "resourceGroup": "<resource-group-name>", // Unique. Only allow up to 90 characters
  "cacheName": "<cache-name>", // Unique. Must be between 3 and 63 characters long. Can only contain numbers, letters, and the - character. The cache name cannot start or end with the - character, and consecutive - characters are not valid.
  "parameters": {
    "location": "<location>",
    "redisVersion": "<redis-version>", // Only 3.0 is available
    "enableNonSslPort": true | false,
    "sku": {
      "name": "<sku-name>",
      "family": "<sku-family>",
      "capacity": <capacity>
    }
  }
}
*/
// Please delete all the comments before using this .json to create service instance.
{
  "resourceGroup": "redisResourceGroup",
  "cacheName": "C0CacheE",
  "parameters": {
    "location": "eastus",
    "redisVersion": "3.0",
    "enableNonSslPort": false,
    "sku": {
      "name": "Basic",
      "family": "C",
      "capacity": 0
    }
  }
}